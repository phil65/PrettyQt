::: prettyqt.itemmodels.MultiColumnFilterProxyModel

This model is used by the [FilterHeader](filterheader.md) widget in order to filter
a table in one go, avoiding the need to layer multiple proxy models.
